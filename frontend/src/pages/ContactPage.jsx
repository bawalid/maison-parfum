import { useState } from "react";
import { FiClock, FiMail, FiMapPin, FiPhone } from "react-icons/fi";
import SectionHeading from "../components/SectionHeading";
import { contactDetails } from "../data/siteContent";
const icons = [FiMapPin, FiPhone, FiMail, FiClock];
const ContactPage = () => { const [submitted, setSubmitted] = useState(false); const handleSubmit = (event) => { event.preventDefault(); setSubmitted(true); }; return (<section className="lux-container py-16"><div className="grid gap-8 lg:grid-cols-[0.95fr_1.05fr]"><div><SectionHeading eyebrow="Contact" title="Contactez notre equipe de conciergerie pour le sourcing, les cadeaux et les conseils olfactifs." text="Que vous choisissiez une signature personnelle ou un cadeau premium, nous proposons une experience parfum sur mesure." /><div className="mt-8 space-y-3">{contactDetails.map((item, index) => { const Icon = icons[index]; return (<div key={item.label} className="lux-card flex items-center gap-4 p-4"><div className="flex h-10 w-10 items-center justify-center text-sm text-gold"><Icon /></div><div><p className="text-[11px] uppercase tracking-[0.2em] text-graphite">{item.label}</p><p className="mt-1 text-base text-ink">{item.value}</p></div></div>); })}</div></div><div className="lux-card"><p className="text-[11px] uppercase tracking-[0.3em] text-gold">Demande privee</p><h2 className="mt-3 font-display text-3xl text-ink">Planifiez un echange avec notre concierge parfum</h2><form className="mt-6 space-y-4" onSubmit={handleSubmit}><label className="block"><span className="lux-label">Nom complet</span><input className="lux-input" type="text" placeholder="Entrez votre nom" /></label><label className="block"><span className="lux-label">Adresse e-mail</span><input className="lux-input" type="email" placeholder="Entrez votre e-mail" /></label><label className="block"><span className="lux-label">Parfum souhaite ou demande</span><textarea className="lux-input min-h-36 resize-none" placeholder="Indiquez le parfum, la famille olfactive ou le type d'accompagnement souhaite" /></label><button type="submit" className="lux-button-primary">Envoyer la demande</button>{submitted ? <p className="text-sm leading-6 text-graphite">Votre demande a bien ete prise en compte. Un conseiller vous recontactera tres bientot.</p> : null}</form></div></div></section>); };
export default ContactPage;